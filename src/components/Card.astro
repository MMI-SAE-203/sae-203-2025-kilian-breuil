---
import ImgPb from './ImgPb.astro'


const { ...film } = Astro.props;
---

<article>
    <a href={`/film/${film.id}`}>
      <ImgPb 
      record={film}
      field="affiche"
      alt={film.titre}
      classPicture="col-span-1 row span-3 flex items-center justify-center"
      classImg="object-cover"/>
    </a>
    <div
      class="relative px-6 py-8 space-y-4"
    >
      <h3 class="text-2xl font-semibold py-4">
        <a class="text-black" href={`/film/${film.id}`}>{film.nom_film}</a>
      </h3>
      <p class="text-gray-500 flex items-center">
        <span class="text-2xl font-bold ">{film.date}</span
        >
      </p>
      <address class="text-gray-500 not-italic  pb-4">
        { film.ba}
      </address>


    </div>

  </article>